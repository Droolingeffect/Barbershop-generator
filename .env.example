# Copy this file to `.env` and fill in your real values.
# Get a key from https://console.cloud.google.com/ — enable "Places API (New)".
GOOGLE_PLACES_API_KEY=your_key_here

# Optional. Defaults to 3000 locally; Vercel sets this automatically in production.
PORT=3000
